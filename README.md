# Простая авторизация на Rust

Поменять данные можно в главном файле:
```rust
    let real_data = methods::UserAuthInfo{
        login: "aboba".to_string(),
        password: "aboba_telecom232".to_string()
    };
```

Удачи!